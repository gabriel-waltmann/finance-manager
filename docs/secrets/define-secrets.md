# Define secrets 
Tip: Secrets normally are stored in ~/.microsoft/usersecrets/<user_secrets_id>/secrets.json

```bash
 dotnet user-secrets set "YourSecretCategory:YourSecretName" "YourSecretValue"
```